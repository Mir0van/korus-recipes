import { defaultTheme } from "./default";

export const antDefaultTheme = {
  token: {
    // Seed Token
    colorPrimary: defaultTheme.colorAccent,
    borderRadius: 2,
    fontFamily: defaultTheme.ffRoboto,
    fontSizeHeading1: 24,
    colorText: defaultTheme.colorText,
    // colorLink: defaultTheme.colorText,
    lineHeight: 1.28571,

    // Alias Token
    // colorBgContainer: '#f6ffed',
  }
};